<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jhonnatan.kalunga">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_cuadrado_kalunga"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_redondo_kalunga"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kalunga"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".core.home.splashScreen.presentation.view.SplashScreenActivity"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>